{
  "name": "framesmith",
  "private": true,
  "type": "module",
  "description": "Beautiful screenshots in the browser. Zero dependencies.",
  "scripts": {
    "test": "node --test \"tests/*.test.js\"",
    "dev": "python -m http.server 5190 -d public",
    "deploy": "npx wrangler pages deploy public --project-name framesmith --branch main"
  }
}
